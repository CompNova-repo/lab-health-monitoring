metrics:
  inode_usage_percentage:
    display_name: "Inode Usage Percentage"
    column_name: "inode_usage_percentage"
    db_type: "DOUBLE PRECISION"
    unit: "%"
    script: "new-metrics/inode_usage_percentage.py"
    command: "df -Pi / | awk 'NR==2 {gsub(/%/, \"\", $5); print $5}'"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  cpu_iowait_percentage:
    display_name: "CPU I/O Wait Percentage"
    column_name: "cpu_iowait_percentage"
    db_type: "DOUBLE PRECISION"
    unit: "%"
    script: "new-metrics/cpu_iowait_percentage.py"
    command: "top -bn1 | awk '/Cpu\\(s\\)/ {gsub(/,/, \"\"); for(i=1;i<=NF;i++) if ($i ~ /wa/) print $(i-1)}'"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  established_connections:
    display_name: Established Connections
    column_name: "established_connections"
    db_type: "INTEGER"
    unit: count
    script: "new-metrics/established_connections.py"
    command: ss -t state established | wc -l
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  time_wait_connections:
    display_name: TIME_WAIT Connections
    column_name: "time_wait_connections"
    db_type: "INTEGER"
    unit: count
    script: "new-metrics/time_wait_connections.py"
    command: ss -t state time-wait | wc -l
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  e2e_load_1m_74080efa:
    display_name: E2E Load Average 1m
    column_name: "e2e_load_1m_74080efa"
    db_type: "DOUBLE PRECISION"
    unit: load
    script: "new-metrics/e2e_load_1m_74080efa.py"
    command: "awk '{print $1}' /proc/loadavg"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  e2e_load_1m_9957f254:
    display_name: E2E Load Average 1m
    column_name: "e2e_load_1m_9957f254"
    db_type: "DOUBLE PRECISION"
    unit: load
    script: "new-metrics/e2e_load_1m_9957f254.py"
    command: "awk '{print $1}' /proc/loadavg"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  e2e_load_1m_58764a2f:
    display_name: E2E Load Average 1m
    column_name: "e2e_load_1m_58764a2f"
    db_type: "DOUBLE PRECISION"
    unit: load
    script: "new-metrics/e2e_load_1m_58764a2f.py"
    command: "awk '{print $1}' /proc/loadavg"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  e2e_load_1m_fe14f35a:
    display_name: E2E Load Average 1m
    column_name: "e2e_load_1m_fe14f35a"
    db_type: "DOUBLE PRECISION"
    unit: load
    script: "new-metrics/e2e_load_1m_fe14f35a.py"
    command: "awk '{print $1}' /proc/loadavg"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  e2e_load_1m_93a5312a:
    display_name: E2E Load Average 1m
    column_name: "e2e_load_1m_93a5312a"
    db_type: "DOUBLE PRECISION"
    unit: load
    script: "new-metrics/e2e_load_1m_93a5312a.py"
    command: "awk '{print $1}' /proc/loadavg"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true

  e2e_load_1m_d1998b7e:
    display_name: E2E Load Average 1m
    column_name: "e2e_load_1m_d1998b7e"
    db_type: "DOUBLE PRECISION"
    unit: load
    script: "new-metrics/e2e_load_1m_d1998b7e.py"
    command: "awk '{print $1}' /proc/loadavg"
    parser: "first_float"
    timeout_seconds: 15
    enabled: true
